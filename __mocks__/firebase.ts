// // __mocks__/firebase.js

// const firebaseMock: {
//     auth: () => {
//         signInWithEmailAndPassword: jest.Mock<Promise<any>, [string, string]>;
//         signOut: jest.Mock<Promise<any>, []>;
//         // Add other functions you need to mock
//     };
//     // Add other Firebase modules you need to mock
// } = jest.createMockFromModule('firebase');

// // Mock the functions you need, for example:
// firebaseMock.auth = jest.fn(() => ({
//     signInWithEmailAndPassword: jest.fn(),
//     signOut: jest.fn(),
// }));

// export default firebaseMock;
